<%= form_with model: [list, item], method: "delete", local: true do |f| %>
    <%= hidden_field_tag 'list_id', list.id %>
    <%= hidden_field_tag 'item_id', item.id %>
    <%= f.submit "Delete Task" %></div>
<% end %>