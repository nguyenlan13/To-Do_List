<%= render 'list', list: @list %>

<% @list.items.each do |item| %>
    <%= render partial: "items/item", locals: {item: item} %>
    <% end %>
    
<%= link_to "Add Tasks", new_list_item_path(@list, @item) %>